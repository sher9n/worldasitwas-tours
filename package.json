{
  "name": "timetravel",
  "private": true,
  "version": "0.1.0",
  "description": "Time Travel tour engine: batch pipeline, tour API, and mobile playground",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "api": "npm run dev -w @timetravel/api",
    "playground": "npm run dev -w @timetravel/playground",
    "pipeline": "npm run cli -w @timetravel/pipeline --",
    "typecheck": "tsc -b packages/schema apps/api apps/pipeline apps/playground",
    "test": "npm run test -w @timetravel/schema && npm run test -w @timetravel/api && npm run test -w @timetravel/pipeline"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.10.2"
  }
}
